// BATCH SEED. Change this string to regenerate the whole batch with fresh copy
// and fresh visual variation. Same seed = identical, reproducible output.
//
// Bump it for each new drop, e.g. '2026-01' -> '2026-02' -> 'launch-week'.
// Every reel's hook, kicker, benefit wording, and mode rotation is derived from
// this seed, so one change here refreshes all 51 reels at once.
export const BATCH_SEED = '2026-01';